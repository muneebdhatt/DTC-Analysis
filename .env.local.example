# Both are PUBLIC by design — the anon key is safe in a browser bundle
# because RLS on these tables grants SELECT only.
#
# The service_role key must NEVER appear here. It lives in n8n and nowhere else.

NEXT_PUBLIC_SUPABASE_URL=https://xdjgeonqtxrkwcszkaty.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
